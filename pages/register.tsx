import React, { Component } from 'react';
import Layout from '@/layouts/Layout';

const Register = () => {
    return (
        <Layout>
            Register
        </Layout>
    );
};

export default Register;